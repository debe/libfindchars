<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.knownhosts</groupId>
  	<artifactId>libfindchars-parent</artifactId>
    <version>0.5.0-jdk25-preview</version>
  	<name>libfindchars parent</name>
  	<description>SIMD optimized character detection library</description>
    <packaging>pom</packaging>
    <url>https://github.com/debe/libfindchars</url>

    <properties>
        <flatten-maven-plugin.version>1.7.3</flatten-maven-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <central-publishing-maven-plugin.version>0.10.0</central-publishing-maven-plugin.version>
    </properties>

  	 <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Dennis Berger</name>
            <email>dbgh@knownhosts.org</email>
            <url>https://github.com/debe</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/debe/libfindchars.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/debe/libfindchars.git</developerConnection>
        <url>https://github.com/debe/libfindchars</url>
    </scm>

	<dependencies>
		<dependency>
	    	<groupId>org.junit.jupiter</groupId>
	   		<artifactId>junit-jupiter-api</artifactId>
	    	<version>5.10.1</version>
	    	<scope>test</scope>
		</dependency>
	    <dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-engine</artifactId>
			<version>5.10.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-runner</artifactId>
			<version>1.10.1</version>
			<scope>test</scope>
		</dependency>
	</dependencies>
	<build>
	    <finalName>${project.artifactId}-${project.version}</finalName>
	    <pluginManagement>
	        <plugins>
	            <plugin>
	                <groupId>org.apache.maven.plugins</groupId>
	                <artifactId>maven-compiler-plugin</artifactId>
	                <version>3.13.0</version>

	                <configuration>
	                    <!-- When compilers say false, they mean true ...
	                         https://issues.apache.org/jira/browse/MCOMPILER-209 -->
	                    <useIncrementalCompilation>false</useIncrementalCompilation>

	                    <maxmem>512m</maxmem>
	                    <meminitial>256m</meminitial>
	                    <encoding>UTF-8</encoding>

	                    <release>25</release>


	                    <!-- IntelliJ needs these https://youtrack.jetbrains.com/issue/IDEA-195472 -->
	                    <source>25</source>
	                    <target>25</target>

	                    <debug>true</debug>
	                    <debuglevel>lines,vars,source</debuglevel>
	                    <!-- [#2413] Make compiler warnings a bit more visible
	                         But don't fail (yet) -->
	                    <compilerArgs>
	                        <arg>--enable-preview</arg>
	                        <arg>--add-modules=ALL-SYSTEM</arg>
	                    </compilerArgs>
	                    <annotationProcessorPaths>
				        <path>
				          <groupId>org.openjdk.jmh</groupId>
				          <artifactId>jmh-generator-annprocess</artifactId>
				          <version>1.37</version>
				        </path>
	      			</annotationProcessorPaths>
	                </configuration>
	            </plugin>
	                <plugin>
      					<groupId>org.apache.maven.plugins</groupId>
				    <artifactId>maven-surefire-plugin</artifactId>
	                <version>3.2.5</version>
				    <configuration>
				      <argLine>--enable-preview --add-modules=jdk.incubator.vector --add-modules=ALL-SYSTEM --enable-native-access=ALL-UNNAMED</argLine>
				    </configuration>
				   <dependencies>
	                <dependency>
	                    <groupId>org.apache.maven.surefire</groupId>
	                    <artifactId>surefire-junit-platform</artifactId>
	                    <version>3.2.5</version>
	                </dependency>
            			</dependencies>
                    </plugin>

	            </plugins>
	    </pluginManagement>
	    <plugins>
	        <!-- Flatten: clean POM for publishing -->
	        <plugin>
	            <groupId>org.codehaus.mojo</groupId>
	            <artifactId>flatten-maven-plugin</artifactId>
	            <version>${flatten-maven-plugin.version}</version>
	            <configuration>
	                <flattenMode>ossrh</flattenMode>
	            </configuration>
	            <executions>
	                <execution>
	                    <id>flatten</id>
	                    <phase>process-resources</phase>
	                    <goals>
	                        <goal>flatten</goal>
	                    </goals>
	                </execution>
	                <execution>
	                    <id>flatten-clean</id>
	                    <phase>clean</phase>
	                    <goals>
	                        <goal>clean</goal>
	                    </goals>
	                </execution>
	            </executions>
	        </plugin>
	    </plugins>
	</build>

	<profiles>
	    <!-- Release profile: source JAR, javadoc JAR, GPG signing, Central publishing -->
	    <profile>
	        <id>release</id>
	        <build>
	            <plugins>
	                <!-- Source JAR -->
	                <plugin>
	                    <groupId>org.apache.maven.plugins</groupId>
	                    <artifactId>maven-source-plugin</artifactId>
	                    <version>${maven-source-plugin.version}</version>
	                    <executions>
	                        <execution>
	                            <id>attach-sources</id>
	                            <goals>
	                                <goal>jar-no-fork</goal>
	                            </goals>
	                        </execution>
	                    </executions>
	                </plugin>

	                <!-- Javadoc JAR -->
	                <plugin>
	                    <groupId>org.apache.maven.plugins</groupId>
	                    <artifactId>maven-javadoc-plugin</artifactId>
	                    <version>${maven-javadoc-plugin.version}</version>
	                    <executions>
	                        <execution>
	                            <id>attach-javadocs</id>
	                            <goals>
	                                <goal>jar</goal>
	                            </goals>
	                        </execution>
	                    </executions>
	                    <configuration>
	                        <release>25</release>
	                        <additionalJOptions>
	                            <additionalJOption>--enable-preview</additionalJOption>
	                            <additionalJOption>--add-modules=jdk.incubator.vector</additionalJOption>
	                        </additionalJOptions>
	                        <encoding>UTF-8</encoding>
	                        <doclint>none</doclint>
	                        <quiet>true</quiet>
	                    </configuration>
	                </plugin>

	                <!-- GPG signing -->
	                <plugin>
	                    <groupId>org.apache.maven.plugins</groupId>
	                    <artifactId>maven-gpg-plugin</artifactId>
	                    <version>${maven-gpg-plugin.version}</version>
	                    <executions>
	                        <execution>
	                            <id>sign-artifacts</id>
	                            <phase>verify</phase>
	                            <goals>
	                                <goal>sign</goal>
	                            </goals>
	                        </execution>
	                    </executions>
	                </plugin>

	                <!-- Central Portal publishing -->
	                <plugin>
	                    <groupId>org.sonatype.central</groupId>
	                    <artifactId>central-publishing-maven-plugin</artifactId>
	                    <version>${central-publishing-maven-plugin.version}</version>
	                    <extensions>true</extensions>
	                    <configuration>
	                        <publishingServerId>central</publishingServerId>
	                        <autoPublish>true</autoPublish>
	                        <waitUntil>published</waitUntil>
	                    </configuration>
	                </plugin>
	            </plugins>
	        </build>
	    </profile>
	</profiles>

	<modules>
		<module>libfindchars-api</module>
		<module>libfindchars-compiler</module>
		<module>libfindchars-csv</module>
		<module>libfindchars-examples</module>
		<module>libfindchars-bench</module>
	</modules>
</project>
